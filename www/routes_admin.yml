admin/dashboard:
  controller: Stat
  action: dashboard
  requires_auth: true
  role: admin

admin/user-list:
  controller: User
  action: userList
  requires_auth: true
  role: admin

admin/studio-list:
  controller: Studio
  action: studioList
  requires_auth: true
  role: admin