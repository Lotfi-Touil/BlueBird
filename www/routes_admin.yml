admin/dashboard:
  controller: Stat
  action: dashboard
  requires_auth: true
  role: admin

admin/user-list:
  controller: User
  action: userList
  requires_auth: true
  role: admin

admin/contact-list:
  controller: Main
  action: contactList
  requires_auth: true
  role: admin

admin/contact-details:
  controller: Main
  action: contactDetails
  requires_auth: true
  role: admin
